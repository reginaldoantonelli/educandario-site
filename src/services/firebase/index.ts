/**
 * Inicialização do Firebase
 * Este arquivo configura apenas os serviços Firebase básicos
 * Para lógica de negócio, veja: src/services/api/
 */

export { app, auth, firestore, storage } from './config';
